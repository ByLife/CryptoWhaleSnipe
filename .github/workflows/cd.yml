---
name: Deploy API

on:
    workflow_dispatch:
      inputs:
        ENVIRONMENT:
          type: choice
          description: 'Environment to deploy to'
          required: true
          options:
            - staging

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Extract branch name
        shell: bash
        run: echo "##[set-output name=branch;]$(echo ${GITHUB_REF#refs/heads/})"
        id: extract_branch
      - name: Update bot
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /home/lux/dreambot/${{ github.event.inputs.ENVIRONMENT }}/DreamBot
            git stash
            git pull
            git checkout ${{ steps.extract_branch.outputs.branch }}
            git stash
            git pull
            docker stop dreambot_${{ github.event.inputs.ENVIRONMENT }} || echo "No container running, building new one"
            docker rm dreambot_${{ github.event.inputs.ENVIRONMENT }} || echo "No container running, building new one"
            docker rmi dreambot_dreambot_${{ github.event.inputs.ENVIRONMENT }} || echo "No image found, building new one"
            docker-compose -f docker-compose-${{ github.event.inputs.ENVIRONMENT }}.yml up -d --build